# PLS-SEM形成性测量模型设计

> **创建日期**：2025-02-07
> **数据源**：`Data/CFMC_for_SEM.csv`（5,971条发布标注记录，其中5,908条为核心隐喻构式样本）
> **实现脚本**：`scripts/Q3_02_PLS_SEM基础模型.py`
> **输出目录**：`Data/` 与 `visualization/`

## 1. 问题背景与理论论证

### 1.1 反映性测量模型的不适用性

反映性测量模型（`=~`语法）要求潜变量的指标间高度共变，即同一潜变量的所有指标应被视为该潜变量的可互换“反映”。表87的因子载荷已暴露该假设的失败：

| 问题 | 表87数据 | 诊断 |
|:-----|:---------|:-----|
| η₂指标量级差异巨大 | conventionality=1.0, cognitive_accessibility=22.7, prototype_distance=-24.1 | 非共变结构 |
| η₃两个指标不显著 | systematicity *p*=0.198, entailment_richness *p*=0.203 | 反映性框架失败 |
| 核心路径β₂不显著 | η₂→η₃: β₂=0.035, *p*=0.334 | 模型错误设定导致 |

### 1.2 形成性指标的理论依据

CFMC标注字段是由标注者独立编码的多维度属性，满足形成性测量的全部判据（详见`为什么是形成性指标.md`）：

**判据一：因果方向**。标注者分别评估常规度、认知通达度等维度，这些维度共同构成（constitute）认知阶段，而非认知阶段的可互换反映（reflection）。

**判据二：指标独立性**。以η₂为例，常规度（社会语言学维度）、认知通达度（认知加工维度）、原型距离（统计计算维度）来自不同编码依据，无需共变。高度常规化的隐喻可以认知通达度低（死隐喻），认知通达度高的隐喻可以原型距离大（非典型但易激活）。

**判据三：指标不可删除**。删除任一指标将改变潜变量的构念含义。例如，去除认知通达度后，“参照点锚定”失去认知加工维度的信息。

**文献支撑**：
- Diamantopoulos & Winklhofer (2001)：形成性指标的构成标准与验证流程
- Hair et al. (2017, 2019)：PLS-SEM在形成性测量中的方法论优势
- Becker et al. (2023)：PLS-SEM的高阶构念与形成性测量最新进展

### 1.3 PLS-SEM的方法论适配性

| 特征 | 反映性测量 | PLS-SEM（本方案） |
|:-----|:--------------|:-----------------|
| 测量模型 | 仅反映性（`=~`） | 支持形成性（Mode.B） |
| 分布假设 | 多元正态 | 无分布假设 |
| 最优化目标 | 协方差矩阵再现 | 方差解释最大化 |
| 样本量要求 | 较高 | 灵活（本研究N=5,971，充裕） |
| 全局拟合指标 | CFI, RMSEA, TLI | GoF, R² |

## 2. 测量模型设计（外部模型）

所有潜变量均采用Mode.B形成性测量，即指标→潜变量。

### 2.1 潜变量与指标配置

| 潜变量 | 认知阶段 | 指标 | 变量名 | 数据类型 | 取值范围 |
|:-------|:---------|:-----|:-------|:---------|:---------|
| η₁ | 域激活 | 具身体验 | embodied_experience | 连续 | [0, 1] |
| | | 源域频率编码 | source_domain_num | 频率编码 | 1-25 |
| | | 目标域频率编码 | target_domain_num | 频率编码 | 1-24 |
| η₂ | 参照点锚定 | 常规度 | conventionality | 连续 | [0.1, 1.0] |
| | | 认知通达度 | cognitive_accessibility | 序数 | 2-5 |
| | | 原型距离 | prototype_distance | 连续 | [0.56, 3.46] |
| η₃ | 跨域映射 | 映射方向 | mapping_direction | 类别 | 1-4 |
| | | 系统性 | systematicity | 连续 | [0.2, 1.0] |
| | | 蕴涵丰富度 | entailment_richness | 连续 | [0.06, 1.0] |
| Y | 语言编码 | 系词功能 | copula_function_num | 类别 | 1-3 |

### 2.2 处理说明

- **标准化**：`scaled=True`，所有指标z-score标准化后进入模型
- **Y的测量模式**：单指标变量使用Mode.A（反映性），权重固定为1.0
- **类别变量处理**：mapping_direction（1-4）和copula_function_num（1-3）为类别编码。当前模型只将mapping_direction视为跨域构型的类别代理，不按线性强弱解释；copula_function_num作为Y和MGA分组依据，解释时同样回到等同式、属性式、识别式三类功能本身。

## 3. 结构模型设计（内部模型）

### 3.1 模型A：完整四阶段模型

对应论文第7章四阶段认知编码机制假设：

```
η₁（域激活）→ η₂（参照点锚定）→ η₃（跨域映射）→ Y（语言编码）
```

路径：
- η₁ → η₂（β₁）：域激活与参照点锚定之间的路径关联
- η₂ → η₃（β₂）：参照点锚定与跨域映射之间的核心路径关联
- η₃ → Y（γ）：跨域映射与语言编码端点之间的边界性关联

### 3.2 模型B：三阶段对照模型

对照模型（省略η₁阶段）：

```
η₂（参照点锚定）→ η₃（跨域映射）→ Y（语言编码）
```

路径：
- η₂ → η₃（β₂）
- η₃ → Y（γ）

### 3.3 模型C：直接效应模型

检验是否存在跳跃路径（即中间阶段是否可绕过）：

```
η₁ → η₂ → η₃ → Y
η₁ --------→ η₃    （直接跳跃路径1）
     η₂ --------→ Y（直接跳跃路径2）
```

路径：
- η₁ → η₂（β₁）
- η₁ → η₃（直接效应）
- η₂ → η₃（β₂）
- η₂ → Y（直接效应）
- η₃ → Y（γ）

## 4. 评估标准

### 4.1 形成性测量模型评估

| 指标 | 标准 | 说明 |
|:-----|:-----|:-----|
| VIF | < 5（理想< 3.3） | 形成性指标间多重共线性 |
| 外部权重显著性 | *p* < 0.05 (bootstrap) | 指标对潜变量的贡献 |
| 外部载荷 | > 0.50 | 权重不显著但载荷>0.50时保留指标 |

**指标保留规则**（Hair et al., 2017）：
1. 外部权重显著（*p* < 0.05）→ 保留
2. 外部权重不显著但外部载荷 > 0.50 → 保留
3. 外部权重不显著且外部载荷 < 0.50 → 理论论证后决定

### 4.2 结构模型评估

| 指标 | 标准 | 说明 |
|:-----|:-----|:-----|
| 路径系数 | *p* < 0.05 (bootstrap 5000次) | 潜变量间关系的统计显著性 |
| R² | > 0.10（弱）, > 0.25（中）, > 0.50（强） | 内生变量解释力 |
| f² | 0.02/0.15/0.35 | 小/中/大效应量 |
| GoF | > 0.25（中）, > 0.36（大） | 全局拟合优度（communality × R²的几何平均） |

### 4.3 Bootstrap设定

- 重抽样次数：5000
- 置信区间类型：百分位法（percentile）
- 显著性水平：双尾 α = 0.05

## 5. 输出文件规划

### 5.1 数据文件（CSV）

| 文件 | 内容 |
|:-----|:-----|
| PLS_路径系数表.csv | 三个模型的路径系数、SE、t、p、CI |
| PLS_外部权重与VIF.csv | 各指标的外部权重、载荷、VIF |
| PLS_模型拟合比较.csv | R²、f²、GoF对比 |
| PLS_Bootstrap结果.csv | Bootstrap路径系数分布统计 |
| PLS_效应分解表.csv | 直接、间接、总效应 |

### 5.2 可视化文件（SVG）

| 文件 | 内容 |
|:-----|:-----|
| visualization/image6.svg | 四阶段路径结构示意图 |
| visualization/image28.svg | PLS-SEM外部权重（含Bootstrap 95% CI） |
| visualization/image29.svg | 效应分解与中介模式图 |
| visualization/image30.svg | 分组路径强度比较图 |

*设计文档版本：1.0*
