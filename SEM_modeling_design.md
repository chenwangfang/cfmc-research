# 认知编码机制SEM建模设计（方案B 11指标）

---

## 一、模型概述

### 1.1 理论模型

认知编码机制采用**方案B配置（11指标）**：三个潜变量（η₁-η₃）对应认知加工的三个核心阶段，copula_function作为η₃的结果观测变量。

**三潜变量与四阶段的关系**：SEM模型的三个潜变量（η₁-η₃）与语言编码输出（X₁₁）**共同构成**CLAUDE.md所述的四阶段认知编码机制。其中，η₁-η₃分别对应阶段1-3的潜在认知过程，X₁₁（copula_function）对应阶段4的语言编码输出。由于阶段4仅有单一观测指标，采用结果变量而非独立潜变量的处理方式，但这不改变四阶段机制的理论完整性。

```
阶段1（认知域激活）→ 阶段2（参照点锚定）→ 阶段3（跨域映射）→ 阶段4（语言编码）
        η₁                    η₂                      η₃                    X₁₁(copula_function)
      潜变量                 潜变量                   潜变量                    结果变量
```

**模型结构**：η₁ → η₂ → η₃ → X₁₁(copula_function)

**关键调整**：
- 语言编码输出不再作为独立潜变量（原方案中的"阶段4"）
- copula_function作为η₃的结果观测变量，体现"映射关系决定系词功能选择"的理论逻辑
- 模型从12指标简化为11指标，避免了语言编码输出仅2指标的识别性问题

**核心假设**：构式网络中12类构式**共享**同一个四阶段认知编码机制。"共享"的操作化定义为测量不变性成立（因子结构跨12类构式保持一致）。

### 1.2 验证目标

| 验证层次 | 验证内容 | 验证标准 |
|:---------|:---------|:---------|
| 第一层：机制存在性 | H3-1的第一层验证 | 整体SEM：CFI > 0.90，RMSEA < 0.08，路径β ≥ 0.40 |
| 第二层：跨类型共享性 | H3-1的第二层验证 | 多组SEM：弱不变性ΔCFI < 0.01 |
| 探索性分析（定量） | 12类构式路径差异 | Wald检验路径系数差异（不设预测阈值） |
| 探索性分析（定量） | 汉语认知特色调节效应 | 多组SEM路径系数比较 |
| 探索性讨论（定性） | 零系词现象 | 理论阐释（不做定量分析） |

---

## 二、测量模型设计

### 2.1 潜变量与指标变量配置

| 潜变量/变量 | 名称 | 指标变量 | 指标数 | 处理方式 | 理论来源 |
|:------------|:-----|:---------|:-------|:---------|:---------|
| η₁ | 认知域激活 | X₁, X₂, X₃ | 3 | 潜变量 | Sullivan + Langacker |
| η₂ | 参照点锚定 | X₄, X₅, X₆ | 3 | 潜变量 | Langacker认知参照点模型（深化Sullivan依存性原则） |
| η₃ | 跨域映射 | X₇, X₈, X₉, X₁₀ | 4 | 潜变量 | Sullivan (2013) 核心原则 |
| X₁₁ | 系词功能（语言编码输出） | — | 1 | η₃的结果变量 | Sullivan系词构式分析 |
| **合计** | | | **11** | | |

### 2.2 指标变量详细定义

#### 阶段1：认知域激活（η₁）

| 指标 | CFMC-33字段 | 操作化定义 | 取值范围 | 变量类型 |
|:-----|:------------|:-----------|:---------|:---------|
| X₁ | embodied_experience | 具身经验强度 | 0-1 | 连续 |
| X₂ | source_domain | 源域类型 | 连续 | 分类→主成分降维 |
| X₃ | target_domain | 目标域类型 | 连续 | 分类→主成分降维 |

**编码说明**：
- X₁（embodied_experience）：0=无具身基础，1=高度具身化
- X₂（source_domain）：15类源域（SP/MV/OB/LV/BD/SN/FC/NT/HM/WR/EC/TR/FD/MC/GM），采用主成分降维后的连续得分
- X₃（target_domain）：10类目标域（TM/LF/EM/TH/SC/MR/AB/CM/ST/EV），采用主成分降维后的连续得分

#### 阶段2：参照点锚定（η₂）

| 指标 | CFMC-33字段 | 操作化定义 | 取值范围 | 变量类型 |
|:-----|:------------|:-----------|:---------|:---------|
| X₄ | conventionality | 常规度（参照点稳定性） | 0-1 | 连续 |
| X₅ | cognitive_accessibility | 认知通达度 | 1-5 | 有序 |
| X₆ | prototype_distance | 原型距离 | 1-3 | 有序 |

**编码说明**：
- X₄（conventionality）：0=低常规度（参照点不稳定），1=高常规度（参照点稳定）。理论依据：常规化程度高的隐喻构式，认知参照点更加稳定、清晰，与Langacker参照点可及性理论一致
- X₅（cognitive_accessibility）：1=极低通达，5=极高通达
- X₆（prototype_distance）：1=中心成员，2=次中心，3=边缘成员

**字段替换说明**：CFMC-33中的`cognitive_reference_point`为String类型（参照点描述），不适合直接用于SEM。本研究以`conventionality`（常规度）作为"参照点稳定性"的操作化指标，因为常规化程度与认知参照点的可及性和稳定性高度相关。

#### 阶段3：跨域映射（η₃）

| 指标 | CFMC-33字段 | 操作化定义 | 取值范围 | 变量类型 |
|:-----|:------------|:-----------|:---------|:---------|
| X₇ | mapping_direction | 映射方向 | 1-4 | 有序 |
| X₈ | mapping_basis | 映射基础 | 连续 | 分类→主成分降维 |
| X₉ | systematicity | 映射系统性 | 0-1 | 连续 |
| X₁₀ | entailment_richness | 蕴涵丰富度 | 0-1 | 连续 |

**编码说明**：
- X₇（mapping_direction）：1=具→具，2=具→抽，3=抽→抽，4=抽→具
- X₈（mapping_basis）：7类（similarity/correlation/function/structure/causality/contiguity/function_similarity），采用主成分降维后的连续得分（与source_domain/target_domain处理方式一致）
- X₉（systematicity）：0=低系统性，1=高系统性
- X₁₀（entailment_richness）：0=蕴涵贫乏，1=蕴涵丰富

#### 语言编码输出（X₁₁：η₃的结果变量）

| 指标 | CFMC-33字段 | 操作化定义 | 取值范围 | 变量类型 |
|:-----|:------------|:-----------|:---------|:---------|
| X₁₁ | copula_function | 系词功能（认知复杂度） | 1-3 | 有序 |

**copula_function编码**：

| 原值 | 重编码 | 认知复杂度 | 理论说明 | 示例 |
|:-----|:-------|:-----------|:---------|:-----|
| equative | 1 | 低（中性等同） | 建立简单等同关系，认知负荷最低 | "人生是舞台" |
| attributive | 2 | 中（属性赋予） | 将属性归于主语，需属性识别过程 | "这想法是错误的" |
| identificational | 3 | 高（身份识别） | 指明主语的身份/角色，涉及复杂推理 | "他是这个团队的灵魂" |

**语料分布**：equative 47.7%，attributive 37.3%，identificational 15.0%

### 2.3 不纳入SEM的字段说明

| 字段 | 不纳入原因 | 替代处理方式 |
|:-----|:-----------|:-------------|
| copula_type | 分类变量（4类），分布严重不平衡 | 作为描述性统计报告 |
| construction_type | Q1分类输出（12类） | 作为多组SEM的分组变量 |
| copula | 名义分类变量（82种系词） | 派生信息已整合到copula_function |
| conceptual_complexity | Q1双维度分类的第二维度 | 通过派生的construction_type间接参与（分组变量） |
| cognitive_reference_point | String类型（参照点描述） | 以conventionality作为"参照点稳定性"的操作化指标 |
| holistic_imagery | 汉语特色调节变量 | 7.5节探索性分析：调节效应检验 |
| relational_thinking | 汉语特色调节变量 | 7.5节探索性分析：调节效应检验 |

---

## 三、多重共线性诊断与效度检验

### 3.1 变量相关性现状

基于5,989条语料的核心变量相关矩阵显示以下高相关对（|*r*| > 0.70）：

| 变量对 | 相关系数 | 所属潜变量 | 性质 |
|:-------|:---------|:-----------|:-----|
| cognitive_accessibility ↔ conventionality | 0.737 | η₂内部 | 收敛效度（正常） |
| conventionality ↔ prototype_distance | -0.695 | η₂内部 | 收敛效度（正常） |
| conventionality ↔ systematicity | 0.701 | η₂ ↔ η₃ | 区分效度（需检验） |
| holistic_imagery ↔ relational_thinking | 0.741 | 调节变量 | 合并处理 |

### 3.2 SEM框架下的理论澄清

**关键洞察**：在SEM测量模型中，同一潜变量的指标**应该**高度相关——这是**收敛效度**的表现，而非多重共线性问题。

```
η₂内部相关（收敛效度，正常现象）：
  cognitive_accessibility ↔ conventionality: r = 0.737 ✓
  cognitive_accessibility ↔ prototype_distance: r = -0.681 ✓
  conventionality ↔ prototype_distance: r = -0.695 ✓

跨潜变量相关（区分效度，需检验）：
  conventionality (η₂) ↔ systematicity (η₃): r = 0.701 ⚠️
```

**处理原则**：
- η₂内部的高相关**不需要**删除任何指标（保持四阶段机制理论完整性）
- η₂与η₃之间的跨潜变量相关需要**区分效度检验**
- 若区分效度检验通过，允许跨潜变量残差相关

### 3.3 收敛效度检验

**检验方法**：因子载荷与平均方差抽取量（AVE）

| 潜变量 | 指标变量 | 预期载荷λ | AVE标准 |
|:-------|:---------|:----------|:--------|
| η₁ | X₁, X₂, X₃ | 0.50-0.70 | ≥ 0.50 |
| η₂ | X₄, X₅, X₆ | 0.55-0.75 | ≥ 0.50 |
| η₃ | X₇, X₈, X₉, X₁₀ | 0.45-0.65 | ≥ 0.50 |

**AVE计算公式**：
$$AVE = \frac{\sum \lambda_i^2}{n}$$

**判断标准**：
- 各指标因子载荷λ ≥ 0.50（可接受）或 ≥ 0.70（理想）
- AVE ≥ 0.50（表明潜变量解释了其指标变量50%以上的方差）

### 3.4 区分效度检验

**检验目的**：确认η₂（参照点锚定）与η₃（跨域映射）是两个独立的潜变量

**检验方法**：嵌套模型比较

```
模型A（二因子）：η₂ 与 η₃ 分开建模（自由估计因子相关）
模型B（单因子）：η₂ 与 η₃ 的指标合并为单一因子

判断标准：
- Δχ² 显著（p < 0.05）
- 模型A的CFI > 模型B的CFI
- AVE(η₂) > r²(η₂,η₃) 且 AVE(η₃) > r²(η₂,η₃)  【Fornell-Larcker标准】
```

**lavaan实现**：

```r
# 二因子模型
model_2factor <- '
  eta2 =~ conventionality + cognitive_accessibility + prototype_distance
  eta3 =~ mapping_direction + mapping_basis_pc + systematicity + entailment_richness
'

# 单因子模型
model_1factor <- '
  combined =~ conventionality + cognitive_accessibility + prototype_distance +
              mapping_direction + mapping_basis_pc + systematicity + entailment_richness
'

# 模型比较
fit_2f <- cfa(model_2factor, data = df)
fit_1f <- cfa(model_1factor, data = df)
anova(fit_2f, fit_1f)  # 卡方差异检验
```

### 3.5 残差相关设定

**若区分效度检验通过**，允许conventionality与systematicity的残差相关，以处理跨潜变量的高相关：

```
修正后的SEM模型：
η₁ → η₂ → η₃ → X₁₁
      ↓         ↓
   conventionality ←→ systematicity（允许残差相关）
```

**lavaan语法**：

```r
model_with_residual_cov <- '
  # 测量模型
  eta1 =~ embodied_experience + source_domain_pc + target_domain_pc
  eta2 =~ conventionality + cognitive_accessibility + prototype_distance
  eta3 =~ mapping_direction + mapping_basis_pc + systematicity + entailment_richness

  # 结构模型
  eta2 ~ beta1*eta1
  eta3 ~ beta2*eta2 + beta3*eta1
  copula_function ~ gamma*eta3

  # 允许残差相关（处理跨潜变量高相关）
  conventionality ~~ systematicity
'
```

### 3.6 多重共线性诊断指标

**VIF检验**（观测变量层面）：

| 指标 | 可接受 | 警戒 | 问题 |
|:-----|:-------|:-----|:-----|
| VIF | < 5 | 5-10 | > 10 |
| 容忍度(1/VIF) | > 0.20 | 0.10-0.20 | < 0.10 |

**条件指数检验**：
- CI < 15：无问题
- CI 15-30：中度共线性
- CI > 30：严重共线性

**Python实现**：

```python
from statsmodels.stats.outliers_influence import variance_inflation_factor
import pandas as pd

def check_multicollinearity(df, variables):
    """计算VIF检验多重共线性"""
    X = df[variables].dropna()
    vif_data = pd.DataFrame()
    vif_data["Variable"] = variables
    vif_data["VIF"] = [variance_inflation_factor(X.values, i) for i in range(len(variables))]
    vif_data["Tolerance"] = 1 / vif_data["VIF"]
    vif_data["Status"] = vif_data["VIF"].apply(
        lambda x: "✓ 正常" if x < 5 else ("⚠️ 警戒" if x < 10 else "✗ 问题")
    )
    return vif_data
```

### 3.7 汉语特色变量处理

**holistic_imagery与relational_thinking**（r = 0.741）不在核心四阶段SEM中，作为调节变量使用。

**处理方案**：合并为"汉语认知特色"复合变量

```python
# 复合变量计算
df['chinese_cognitive_style'] = (df['holistic_imagery'] + df['relational_thinking']) / 2

# 用途：作为调节变量，检验其对四阶段路径的调节效应
```

**理论依据**：
- 两变量均反映汉语认知特色（整体性思维、关系性思维）
- 高相关（r = 0.741）表明它们测量相近的认知维度
- 合并后作为单一调节变量，避免多重检验问题

### 3.8 诊断流程总结

```
┌─────────────────────────────────────────────────────────────────┐
│                    多重共线性与效度检验流程                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  步骤1：VIF诊断                                                  │
│  ├─ 观测变量VIF < 10 ────────────────────────→ 通过             │
│  └─ 观测变量VIF ≥ 10 ────────────────────────→ 考虑变量处理     │
│                                                                 │
│  步骤2：收敛效度检验                                              │
│  ├─ 因子载荷λ ≥ 0.50 且 AVE ≥ 0.50 ─────────→ 通过             │
│  └─ 不满足 ───────────────────────────────────→ 检查指标分配     │
│                                                                 │
│  步骤3：区分效度检验                                              │
│  ├─ Δχ² 显著 且 Fornell-Larcker通过 ─────────→ η₂与η₃独立      │
│  └─ 不满足 ───────────────────────────────────→ 考虑合并或重新建模│
│                                                                 │
│  步骤4：残差相关设定（若区分效度通过）                              │
│  └─ 允许conventionality ~~ systematicity                        │
│                                                                 │
│  步骤5：汉语特色变量处理                                          │
│  └─ 合并为chinese_cognitive_style复合调节变量                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、结构模型设计

### 4.1 路径设定

```
η₁ ──β₁──→ η₂ ──β₂──→ η₃ ──γ──→ X₁₁(copula_function)
         ↘_________β₃_________↗
           （跨阶段直接效应，用于中介效应检验）
```

**路径假设**：
- β₁：认知域激活 → 参照点锚定
- β₂：参照点锚定 → 跨域映射
- γ：跨域映射 → 系词功能选择（语言编码输出）
- β₃：认知域激活 → 跨域映射（跨阶段直接效应，用于中介效应检验）

**符号说明**：β用于潜变量间路径，γ用于潜变量到观测变量的回归路径，符合SEM符号惯例。

**路径解释**：箭头"→"表示**逻辑依赖顺序**，而非简单的时间先后因果效应。SEM路径系数（β）反映阶段间的方向性关联强度和逻辑依赖程度。四阶段机制应理解为"具有逻辑依赖顺序的动态交互过程"，各阶段在实际认知加工中可**级联进行**（cascaded processing），后续阶段可在前序阶段尚未完全完成时开始激活。

### 4.2 模型识别

**方案B优势**：
- η₁-η₃各有≥3个指标，满足"每潜变量3指标"的识别性要求
- X₁₁作为η₃的结果变量（回归路径），无识别性问题
- 避免了原方案中阶段4仅2指标需要固定因子载荷的约束

### 4.3 路径图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│               认知编码机制SEM路径模型（方案B：11指标）                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────┐      β₁        ┌─────┐      β₂        ┌─────┐      γ         ┌─────┐
│   │ η₁  │ ────────────→ │ η₂  │ ────────────→ │ η₃  │ ────────────→ │ X₁₁ │
│   │认知域│               │参照点│               │自主- │               │copula│
│   │奠基  │               │确立  │               │依存  │               │funct-│
│   │     │               │     │               │映射  │               │ion   │
│   └──┬──┘               └──┬──┘               └──┬──┘               └─────┘
│      │                     │                     ↑
│      └─────────────────────┼──────β₃─────────────┘
│                            │           （跨阶段直接效应，用于中介效应检验）
│   ┌─────┐               ┌──┴──┐               ┌─────┐
│   │X₁X₂X₃│              │X₄X₅X₆│              │X₇-X₁₀│
│   └─────┘               └─────┘               └─────┘
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ 指标变量说明：                                                               │
│ X₁=embodied_experience   X₅=cognitive_accessibility  X₉=systematicity       │
│ X₂=source_domain         X₆=prototype_distance       X₁₀=entailment_richness│
│ X₃=target_domain         X₇=mapping_direction        X₁₁=copula_function    │
│ X₄=conventionality       X₈=mapping_basis            (η₃的结果变量)          │
├─────────────────────────────────────────────────────────────────────────────┤
│ 路径符号说明：                                                               │
│ β₁, β₂, β₃ = 潜变量间路径系数；γ = 潜变量到观测变量的回归系数                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.4 中介效应检验设计

**检验目的**：检验η₂（参照点锚定）在η₁→η₃路径中的中介作用，判断是完全中介还是部分中介。

**中介路径设定**：

```
路径1（串行中介）：η₁ → η₂ → η₃ → X₁₁
路径2（直接+中介）：η₁ → η₃ → X₁₁（跳过η₂）
路径3（间接效应）：η₂ → η₃ → X₁₁（阶段2对结果变量的间接效应）
```

**核心中介效应检验**：

| 中介路径 | 路径说明 | 效应计算 | 理论意义 |
|:---------|:---------|:---------|:---------|
| η₁→η₂→η₃ | 认知域激活→参照点锚定→跨域映射 | β₁×β₂ | 参照点锚定在域激活与映射间的桥梁作用 |
| η₂→η₃→X₁₁ | 参照点锚定→跨域映射→copula_function | β₂×γ | 映射关系在参照点与语言编码间的桥梁作用 |
| η₁→η₂→η₃→X₁₁ | 完整串行路径 | β₁×β₂×γ | 认知编码的完整级联效应 |

**效应分解**：

| 效应类型 | 计算方法 | 检验方法 |
|:---------|:---------|:---------|
| 总效应 | c = β₁×β₂×γ + β₃×γ | — |
| 直接效应 | c' = β₃×γ | z检验 |
| 间接效应 | a×b = β₁×β₂×γ | Bootstrap（5,000次重采样） |
| 中介比例 | 间接效应/总效应 | — |

**判断标准**：
- 间接效应Bootstrap 95% CI不包含0 → 中介效应显著
- 直接效应（β₃）显著 → **部分中介**
- 直接效应（β₃）不显著 → **完全中介**
- **预期结果**：η₂的链式中介效应显著，中介比例≥60%，β₃显著但较小（0.15-0.25），支持部分中介模型

**理论意义**：
- 若为完全中介：认知域激活**必须**经由参照点锚定才能影响跨域映射
- 若为部分中介：认知域激活对跨域映射有直接通道，同时参照点锚定发挥重要桥梁作用
- 预期部分中介更符合认知加工的级联特征——后续阶段可在前序阶段尚未完成时开始激活

---

## 五、估计方法

### 5.1 估计器选择

| 估计器 | 适用条件 | 本研究适用性 |
|:-------|:---------|:-------------|
| **ML（多分相关矩阵）** | 连续/有序混合数据 | **首选**（10个指标+1结果变量） |
| WLSMV | 有序分类数据 | 备选（若ML不收敛） |
| MLR | 稳健标准误 | 备选（数据非正态时） |

**推荐**：基于多分相关矩阵的ML估计，Bootstrap方法（5,000次）校正标准误

### 5.2 样本量要求

| 估计方法 | 最低样本量 | 推荐样本量 | 本研究样本 |
|:---------|:-----------|:-----------|:-----------|
| ML | 200 | 500+ | 5,989 ✓ |
| 多组SEM（12组） | 100/组 | 200/组 | ~499/组 ✓ |

### 5.3 拟合指标标准

| 指标 | 可接受 | 良好 | 优秀 |
|:-----|:-------|:-----|:-----|
| CFI | ≥ 0.90 | ≥ 0.95 | ≥ 0.97 |
| TLI | ≥ 0.90 | ≥ 0.95 | ≥ 0.97 |
| RMSEA | ≤ 0.08 | ≤ 0.06 | ≤ 0.05 |
| SRMR | ≤ 0.08 | ≤ 0.06 | ≤ 0.05 |

---

## 六、多组SEM设计

### 6.1 分组变量

- **分组变量**：construction_type（12类构式）
- **分组来源**：Q1分类结果（认知通达度3级 × 映射类型4类）
- **每组样本量**：理论均值约499条（5,989/12），实际预计300-650条/组

**样本量分布说明**：根据认知分工原则，高通达-高复杂类构式（如"高通达+具体→抽象"）预期频率较高（约30%-40%），低通达-低复杂类构式预期频率较低（约5%-10%）。因此各组样本量将呈不均衡分布，但即使最小组（约250条）仍满足多组SEM的推荐样本量要求（≥200/组）。

**映射类型与概念复杂度的关系**：映射类型（4类：具体→抽象/抽象→抽象/具体→具体/抽象→具体）是概念复杂度的主要**操作化分类**，但两者不等同。概念复杂度是理论层面的连续变量（1-5级量表），映射类型是操作层面的分类变量。12类构式 = 3级认知通达度 × 4类映射类型（映射类型作为概念复杂度的操作化分类）。

### 6.2 不变性检验层级

| 层级 | 名称 | 约束内容 | 判断标准 |
|:-----|:-----|:---------|:---------|
| M0 | 形态不变性 | 因子结构相同 | 模型可识别 |
| M1 | 弱不变性（载荷不变） | 因子载荷相等 | ΔCFI < 0.01 |
| M2 | 强不变性（截距不变） | 载荷+截距相等 | ΔCFI < 0.01 |
| M3 | 严格不变性（残差不变） | 载荷+截距+残差相等 | ΔCFI < 0.01 |

**H3-1验证标准**：
- 机制存在性：整体模型拟合良好（CFI > 0.90）
- 跨类型共享性：弱不变性成立（M1的ΔCFI < 0.01）

---

## 七、数据预处理要求

### 7.1 缺失值处理

| 缺失比例 | 处理方式 |
|:---------|:---------|
| < 5% | 全信息最大似然（FIML） |
| 5-15% | 多重插补（MICE，m=20） |
| > 15% | 剔除该观测 |

### 7.2 变量转换

| 原始字段 | 转换方式 | 目标变量 | 代码中变量名 |
|:---------|:---------|:---------|:-------------|
| source_domain | 虚拟编码→PCA | X₂（连续） | source_domain_pc |
| target_domain | 虚拟编码→PCA | X₃（连续） | target_domain_pc |
| mapping_basis | 虚拟编码→PCA | X₈（连续） | mapping_basis_pc |
| copula_function | 重编码为1-3级 | X₁₁（有序） | copula_function |

**PCA处理说明**：对分类变量（source_domain 15类、target_domain 10类、mapping_basis 7类）先进行虚拟编码（dummy coding），再对虚拟变量进行主成分分析（PCA），提取第一主成分得分作为SEM指标变量。

### 7.3 异常值处理

- 马氏距离检测（χ² p < 0.001）
- 识别但不自动剔除，进行敏感性分析

---

## 八、软件实现

### 8.1 推荐软件

| 软件 | 优势 | 适用场景 |
|:-----|:-----|:---------|
| **lavaan (R)** | 开源免费，语法灵活，多分相关支持好 | **首选** |
| semopy (Python) | Python生态，与pandas/numpy集成 | 备选 |
| Mplus | 商业软件，WLSMV支持最佳 | 备选 |

### 8.2 lavaan语法示例

```r
# 方案B：11指标SEM模型（含中介效应检验）
library(lavaan)

# 模型定义
model <- '
  # 测量模型
  eta1 =~ embodied_experience + source_domain_pc + target_domain_pc
  eta2 =~ conventionality + cognitive_accessibility + prototype_distance
  eta3 =~ mapping_direction + mapping_basis_pc + systematicity + entailment_richness

  # 结构模型（含跨阶段直接效应）
  eta2 ~ beta1*eta1
  eta3 ~ beta2*eta2 + beta3*eta1    # beta3为跨阶段直接效应
  copula_function ~ gamma*eta3       # gamma为潜变量到观测变量的回归系数

  # 中介效应定义
  indirect := beta1*beta2*gamma      # 间接效应（通过eta2中介）
  direct := beta3*gamma              # 直接效应（跳过eta2）
  total := indirect + direct         # 总效应
  mediation_ratio := indirect/total  # 中介比例
'

# 模型拟合（注：source_domain_pc, target_domain_pc, mapping_basis_pc为主成分降维后的连续变量）
fit <- sem(model, data = df, ordered = c("cognitive_accessibility", "prototype_distance",
                                          "mapping_direction", "copula_function"),
           estimator = "ML", se = "bootstrap", bootstrap = 5000)

# 结果输出
summary(fit, standardized = TRUE, fit.measures = TRUE, ci = TRUE)
```

### 8.3 Python (semopy)语法示例

```python
import semopy
import pandas as pd
import numpy as np

# 模型定义（注：source_domain_pc, target_domain_pc, mapping_basis_pc为主成分降维后的连续变量）
model_desc = """
# 测量模型
eta1 =~ embodied_experience + source_domain_pc + target_domain_pc
eta2 =~ conventionality + cognitive_accessibility + prototype_distance
eta3 =~ mapping_direction + mapping_basis_pc + systematicity + entailment_richness

# 结构模型（含跨阶段直接效应）
eta2 ~ eta1
eta3 ~ eta2 + eta1
copula_function ~ eta3
"""

# 模型拟合
model = semopy.Model(model_desc)
model.fit(df)

# 结果输出
print(model.inspect())

# 中介效应检验（Bootstrap方法）
def bootstrap_mediation(df, n_bootstrap=5000):
    """Bootstrap方法计算中介效应置信区间"""
    indirect_effects = []
    for _ in range(n_bootstrap):
        sample = df.sample(n=len(df), replace=True)
        model_boot = semopy.Model(model_desc)
        model_boot.fit(sample)
        params = model_boot.inspect()
        # 提取路径系数并计算间接效应
        # ... (根据实际参数名称提取)
    return np.percentile(indirect_effects, [2.5, 97.5])
```

---

## 九、预期结果与解读

### 9.1 测量模型预期

| 指标 | 预期因子载荷 | 可接受范围 |
|:-----|:-------------|:-----------|
| X₁-X₃ (η₁) | 0.50-0.70 | ≥ 0.40 |
| X₄-X₆ (η₂) | 0.55-0.75 | ≥ 0.40 |
| X₇-X₁₀ (η₃) | 0.45-0.65 | ≥ 0.40 |

### 9.2 结构模型预期

| 路径 | 预期系数 | 理论预测 |
|:-----|:---------|:---------|
| η₁ → η₂ (β₁) | 0.40-0.50 | 认知域激活质量影响参照点建立 |
| η₂ → η₃ (β₂) | 0.50-0.65 | Sullivan依存性原则核心路径，预期**最高** |
| η₃ → X₁₁ (γ) | 0.40-0.55 | 映射关系决定系词功能选择 |
| η₁ → η₃ (β₃) | 0.15-0.25 | 跨阶段直接效应（用于中介效应检验） |

**预期依据**：β₂（η₂→η₃）预期最高，印证Sullivan (2013)依存性原则在认知编码中的核心地位——认知参照点的确立对跨域映射具有决定性影响。β₃预期较小但显著，支持部分中介模型（认知域激活对映射有直接通道，但主要效应通过参照点锚定中介传递）。

**路径强度顺序预期**：β₂ > γ ≈ β₁ > β₃

**理论解释**：
- β₂最高：体现Sullivan依存性原则的核心地位，参照点锚定是连接认知域激活与跨域映射的关键桥梁
- β₃较小但显著：支持级联加工特征，后续阶段可在前序阶段尚未完成时开始激活

### 9.3 多组比较预期

| 构式类型 | 路径强度预期 | 理论解释 |
|:---------|:-------------|:---------|
| 中心构式 | β均值最高（≈0.55-0.65） | 加工流畅，认知固化程度高 |
| 次中心构式 | β均值中等（≈0.45-0.55） | 部分阶段需额外认知资源 |
| 边缘构式 | β均值较低（≈0.40-0.50） | 规约化程度低，加工难度增加 |

**核心结论**：中心、次中心、边缘构式**共享同一认知编码机制**（测量不变性成立），但**路径强度存在梯度差异**。这一发现支持Sullivan (2013)依存性原则的普适性，同时揭示了原型距离对加工效率的调节作用。

---

## 十、修订记录

| 版本 | 日期 | 修订内容 |
|:-----|:-----|:---------|
| v1.0 | 2025-12-02 | 初始版本，确定阶段4采用双指标配置 |
| v2.0 | 2025-12-02 | **方案B更新**：三潜变量+一结果变量（11指标），阶段4不再作为独立潜变量，copula_function作为η₃的结果变量，移除copula_markedness |
| v2.1 | 2025-12-03 | **一致性修订**：(1) mapping_basis改用主成分降维；(2) 补充"共享"核心概念及操作化定义；(3) 假设编号改为验证层次；(4) η₂理论来源补充"深化Sullivan"；(5) 补充路径逻辑依赖顺序说明；(6) 扩展不纳入SEM字段表格；(7) 增加Sullivan术语对照；(8) 补充核心结论；(9) 新增附录D探索性分析设计 |
| v2.2 | 2025-12-03 | **探索性分析调整**：明确2项定量探索性分析（路径差异Wald检验、汉语认知特色调节）+1项定性探索性讨论（零系词现象）；删除"系词选择与构式类型关联"分析项；本研究聚焦核心系词"是"字构式 |
| v2.3 | 2025-12-03 | **全面审核修订**：(1) X₂/X₃变量类型统一为"分类→主成分降维"；(2) 删除不纳入SEM字段表中cognitive_accessibility误列行；(3) 1.1节补充三潜变量与四阶段关系说明；(4) 5.1节补充映射类型与概念复杂度的操作化关系；(5) 附录B术语对照表增加中文译名列（述谓式、指称式、等同式） |
| v2.4 | 2025-12-03 | **CFMC-33字段对齐**：(1) X₄从cognitive_reference_point改为conventionality（常规度→参照点稳定性），因CFMC-33中cognitive_reference_point为String类型；(2) 删除copula_markedness遗留条目（CFMC-33中不存在）；(3) 不纳入SEM字段表增加cognitive_reference_point说明 |
| v2.5 | 2025-12-03 | **细节完善**：(1) 6.2节变量转换表增加"代码中变量名"列，补充PCA处理说明（虚拟编码→PCA→提取第一主成分）；(2) 5.1节补充样本量不均衡分布说明（预计250-600条/组）；(3) 8.2节补充路径系数预期的理论依据（认知加工链效应衰减） |
| v2.6 | 2025-12-03 | **与提纲一致性修订**：(1) 路径符号统一：η₃→X₁₁从β₃改为γ（符合SEM惯例）；(2) 增加η₁→η₃直接路径（β₃）用于中介效应检验；(3) 新增3.4节中介效应检验设计（Bootstrap 5,000次、完全/部分中介判断标准）；(4) 8.2节修订理论预测方向：β₂最高（Sullivan依存性原则核心），取代原"效应衰减"假设；(5) 更新lavaan/semopy代码示例，加入跨阶段直接效应和中介效应计算 |
| v2.7 | 2025-12-05 | **多重共线性处理方案**：(1) 新增第三章"多重共线性诊断与效度检验"；(2) 澄清η₂内部高相关为收敛效度正常现象（不删除cognitive_accessibility）；(3) 新增η₂与η₃区分效度检验方法（嵌套模型比较、Fornell-Larcker标准）；(4) 新增残差相关设定（conventionality ~~ systematicity）；(5) holistic_imagery与relational_thinking合并为chinese_cognitive_style复合调节变量；(6) 新增VIF诊断代码与诊断流程图；(7) 后续章节编号顺延（三→四...十→十一） |

---

## 十一、附录

### 附录A：方案B设计理据

**为什么采用方案B（11指标）而非原方案（12指标）**：

1. **模型识别性更优**：原阶段4仅2个指标（copula_function, copula_markedness），需固定因子载荷才能识别。方案B中η₁-η₃各有≥3个指标，无需额外约束。

2. **理论逻辑更清晰**：copula_function作为η₃的结果变量，直接体现"跨域映射→系词功能选择"的因果逻辑，符合Sullivan (2013)的理论框架。

3. **避免多重共线性**：原方案中copula_function与copula_markedness高度相关（派生自同一字段copula），可能导致共线性问题。

4. **简约原则**：11指标模型更简洁，参数更少，模型拟合更稳定。

### 附录B：copula_function有序性依据

copula_function的三类（equative/attributive/identificational）反映认知复杂度的有序梯度：

| 类型 | 认知操作 | 复杂度等级 |
|:-----|:---------|:-----------|
| equative（等同式） | 建立简单等同关系 | 低（1） |
| attributive（属性式） | 属性识别与归属 | 中（2） |
| identificational（指称式） | 身份推理与确认 | 高（3） |

这一有序性得到Sullivan (2013: 104-110)系词构式分析的理论支持，以及已标注语料分布的实证支持（equative 47.7% > attributive 37.3% > identificational 15.0%）。

**与Sullivan原著术语对照**：

| 本研究术语 | 中文译名 | Sullivan术语 | Mikkelsen (2005)术语 | 对应关系 |
|:-----------|:---------|:-------------|:--------------------|:---------|
| equative | 等同式 | Equative | Equative | 完全对应 |
| attributive | 属性式 | — | Predicational（述谓式） | 概念相近（将属性归于NP₁） |
| identificational | 指称式 | — | Specificational（指称式） | 概念相近（指明NP₂的所指） |

**说明**：Sullivan (2013: 104-110)援引Mikkelsen (2005)的三分类——Predicational（述谓式）、Specificational（指称式）、Equative（等同式），本研究采用更直观的术语命名（attributive/identificational/equative），但保持概念对应关系。Mikkelsen术语的中文译名参照汉语语法学通行译法。

### 附录C：与原方案的对照

| 维度 | 原方案（12指标） | 方案B（11指标） |
|:-----|:-----------------|:----------------|
| 潜变量数 | 4（η₁-η₄） | 3（η₁-η₃） |
| 指标数 | 12 | 11（10测量+1结果） |
| 阶段4处理 | 独立潜变量（2指标） | η₃的结果变量 |
| copula_markedness | 纳入 | 移除 |
| 识别约束 | 需固定η₄载荷 | 无需额外约束 |
| 结构模型 | η₁→η₂→η₃→η₄ | η₁→η₂→η₃→X₁₁ |

### 附录D：探索性分析设计

#### D.1 路径差异Wald检验（7.3节支撑）

**分析目的**：探索12类构式在四阶段编码路径上的差异模式

**分析方法**：
1. 在多组SEM框架下，对各路径系数进行组间Wald检验
2. 检验统计量：$W = (\hat{\beta}_i - \hat{\beta}_j)^2 / [SE(\hat{\beta}_i)^2 + SE(\hat{\beta}_j)^2]$
3. 显著性判断：Wald χ² (df=1)，*p* < 0.05

**报告内容**：
- 路径系数差异显著的构式对
- 差异模式的描述性归纳
- 不设预测阈值，仅作探索性描述

#### D.2 汉语认知特色调节效应（7.5节支撑）

**调节变量**：
- holistic_imagery（整体成像）
- relational_thinking（关系思维）

**分析方法**：
1. 多组SEM：按调节变量高低分组（中位数分割）
2. 路径系数比较：检验高低组间β差异
3. 交互效应检验：调节变量 × 路径的交互项

**预期发现**：
- holistic_imagery高分组可能在η₁→η₂路径上表现更强
- relational_thinking高分组可能在η₂→η₃路径上表现更强

#### D.3 零系词现象讨论（7.6节支撑，定性探索）

**说明**：本研究聚焦核心系词"是"字构式，同时将零系词现象纳入分析范围。零系词现象采用**定性讨论**方式，不进行定量统计分析。

**讨论内容**：
1. 零系词现象在汉语系表隐喻构式中的分布特征（描述性统计）
2. 零系词现象的认知动因分析（基于Sullivan依存性原则）
3. 零系词与显性系词在认知编码机制上的理论差异
4. 汉语主题突出语言特征对零系词现象的影响

**理论视角**：
- Sullivan (2013)系词构式分析假设系词显性存在，零系词现象对此构成理论扩展
- Langacker认知参照点模型可解释零系词语境下的认知通达机制
- 汉语类型学特色（主题突出、语境依赖）为零系词现象提供语言学基础
